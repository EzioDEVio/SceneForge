aiofiles==25.1.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
fastapi==0.141.1
greenlet==3.5.6
h11==0.16.0
httptools==0.8.0
idna==3.19
pydantic==2.13.5
pydantic_core==2.46.5
python-dotenv==1.2.3
python-multipart==0.0.32
PyYAML==6.0.3
requests==2.34.2
keyring==25.6.0
SQLAlchemy==2.0.53
sse-starlette==3.4.11
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.53.0
watchfiles==1.2.0
websockets==17.1
# NOTE: uvloop is intentionally excluded — it does not build/install on
# Windows (Unix/macOS only). uvicorn falls back to the standard asyncio
# event loop without it, which is not a meaningful bottleneck here since
# almost all real work happens in FFmpeg subprocesses, not the event loop.

huggingface_hub==1.31.0
Pillow>=10,<13

# Colour grading: bakes adjustment sliders and imported .cube LUTs into one 3D LUT.
numpy==2.5.3
# Photo tools (2.5D parallax background fill, photo restore). Headless: no GUI libraries.
opencv-python-headless==5.0.0.93
arabic-reshaper==3.0.1
python-bidi==0.6.11
